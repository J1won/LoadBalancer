#ifndef WEBSERVER_H
#define WEBSERVER_H

#include "Request.h"
#include <string>   
using namespace std;


//takes the request and processes for given time and empties when done.
class Webserver{
public:
     //constructor accepts amount of servers
          // allocate array with server amount of space
          // creates server/processor <server> amount of times
          // and add to the array

     // deallocate array

     // accept a request()
     // give request to idle processor

     //all servers empty()

private:


};
#endif

